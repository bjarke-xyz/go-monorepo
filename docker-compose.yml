version: "3.7"
services:
  benzinpriser:
    image: registry.bjarke.xyz/benzinpriser
    build:
      context: ./
    volumes:
      - /tmp:/tmp
    networks:
      - swarm-overlay

networks:
  swarm-overlay:
    external: true
    name: swarm-overlay
